---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: monitors.monitoring.xisnove.io
spec:
  group: monitoring.xisnove.io
  names:
    kind: Monitor
    listKind: MonitorList
    plural: monitors
    shortNames:
    - xmon
    singular: monitor
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .status.health.state
      name: Health
      type: string
    - jsonPath: .status.externalID
      name: External ID
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              description:
                maxLength: 2048
                type: string
              displayOrder:
                format: int32
                minimum: 0
                type: integer
              failureThreshold:
                format: int32
                maximum: 100
                minimum: 1
                type: integer
              intervalSeconds:
                format: int32
                maximum: 86400
                minimum: 5
                type: integer
              labels:
                additionalProperties:
                  type: string
                maxProperties: 64
                type: object
              locationID:
                description: LocationID is the immutable public control-plane location
                  identifier.
                format: uuid
                type: string
              name:
                description: Name defaults to metadata.name when omitted.
                maxLength: 200
                type: string
              probe:
                properties:
                  dns:
                    properties:
                      expectedValues:
                        items:
                          type: string
                        maxItems: 20
                        type: array
                      name:
                        maxLength: 253
                        minLength: 1
                        type: string
                      recordType:
                        enum:
                        - A
                        - AAAA
                        - CNAME
                        - MX
                        - NS
                        - TXT
                        - SRV
                        type: string
                      resolver:
                        maxLength: 261
                        type: string
                    required:
                    - name
                    - recordType
                    type: object
                  http:
                    properties:
                      body:
                        maxLength: 5464
                        type: string
                      bodyContains:
                        items:
                          type: string
                        maxItems: 20
                        type: array
                      bodyDoesNotContain:
                        items:
                          type: string
                        maxItems: 20
                        type: array
                      expectedStatus:
                        items:
                          description: StatusRange is an inclusive HTTP status range.
                          properties:
                            maximum:
                              format: int32
                              maximum: 599
                              minimum: 100
                              type: integer
                            minimum:
                              format: int32
                              maximum: 599
                              minimum: 100
                              type: integer
                          required:
                          - maximum
                          - minimum
                          type: object
                        maxItems: 20
                        minItems: 1
                        type: array
                      followRedirects:
                        type: boolean
                      headers:
                        additionalProperties:
                          type: string
                        maxProperties: 100
                        type: object
                      method:
                        default: GET
                        enum:
                        - GET
                        - HEAD
                        - POST
                        - PUT
                        - PATCH
                        - DELETE
                        - OPTIONS
                        type: string
                      tlsMinimumRemainingSeconds:
                        format: int64
                        maximum: 31536000
                        minimum: 1
                        type: integer
                      url:
                        maxLength: 4096
                        minLength: 1
                        type: string
                    required:
                    - expectedStatus
                    - url
                    type: object
                  kind:
                    enum:
                    - http
                    - tcp
                    - dns
                    type: string
                  tcp:
                    properties:
                      expect:
                        maxLength: 5464
                        type: string
                      host:
                        maxLength: 253
                        minLength: 1
                        type: string
                      port:
                        format: int32
                        maximum: 65535
                        minimum: 1
                        type: integer
                      send:
                        maxLength: 5464
                        type: string
                      tlsMinimumRemainingSeconds:
                        format: int64
                        maximum: 31536000
                        minimum: 1
                        type: integer
                    required:
                    - host
                    - port
                    type: object
                required:
                - kind
                type: object
                x-kubernetes-validations:
                - message: exactly one probe configuration must match kind
                  rule: (self.kind == 'http' && has(self.http) && !has(self.tcp) &&
                    !has(self.dns)) || (self.kind == 'tcp' && has(self.tcp) && !has(self.http)
                    && !has(self.dns)) || (self.kind == 'dns' && has(self.dns) &&
                    !has(self.http) && !has(self.tcp))
              public:
                type: boolean
              recoveryThreshold:
                format: int32
                maximum: 100
                minimum: 1
                type: integer
              requiredLocation:
                type: boolean
              timeoutMillis:
                format: int32
                maximum: 120000
                minimum: 100
                type: integer
            required:
            - failureThreshold
            - intervalSeconds
            - locationID
            - probe
            - recoveryThreshold
            - requiredLocation
            - timeoutMillis
            type: object
          status:
            properties:
              conditions:
                description: Conditions contains only bounded current state; incident
                  history remains in the control plane.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                maxItems: 8
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              externalID:
                description: ExternalID is the identifier of the operator-owned control-plane
                  object.
                type: string
              health:
                properties:
                  lastTransitionTime:
                    format: date-time
                    type: string
                  state:
                    enum:
                    - unknown
                    - up
                    - degraded
                    - down
                    type: string
                type: object
              observedGeneration:
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
